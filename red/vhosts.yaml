name: VHost Enumeration with ffuf
description:
commands:
  - command: |
      ffuf -c -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -H "Host: FUZZ.$DOMAIN" -u http://$DOMAIN -fs 178
    description: VHost Enumeration
tags:
  - enumeration
  - web
show:
links:
  - ffuf
references:
  -

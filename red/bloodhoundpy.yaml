name: BloodHound.py
description: BloodHound.py is a Python based ingestor for BloodHound, based on Impacket. It allows you to remotely collect data for bloodhound by querying LDAP
commands:
  - command: bloodhound.py -u $USER -p $PASSWORD -d $DOMAIN -v --zip -c All -dc $DOMAIN -ns $IP
    description: Execute SharpHound locally on a compromised system. (Noisy)
tags:
  - enumeration
  - exploitation
  - ldap
show:
links:
  - sharphound
references:
  - https://github.com/dirkjanm/BloodHound.py
